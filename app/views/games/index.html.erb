<% provide(:title, 'All Games') %>
<h1>All Games</h1>

<%= will_paginate %>
<ul class="games">
  <% @games.each do |game| %>
    <li>
      <h4>
      <%= link_to game.name, game %>
      </h4>
    </li>
  <% end %>
</ul>
<%= will_paginate %>
